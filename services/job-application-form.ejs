<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Application - Orb Nexus</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #0f0f0f;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .form-container {
            background: rgba(20, 20, 20, 0.9);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(255, 215, 0, 0.2);
            width: 90%;
            max-width: 500px;
        }
        h1 {
            text-align: center;
            font-size: 24px;
            color: gold;
            margin-bottom: 20px;
        }
        label {
            font-weight: 600;
            display: block;
            margin: 10px 0 5px;
        }
        input, button {
            width: 100%;
            padding: 10px;
            border-radius: 8px;
            border: none;
            font-size: 16px;
        }
        input {
            background: #1a1a1a;
            color: white;
            border: 1px solid #444;
        }
        button {
            background: gold;
            color: black;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }
        button:hover {
            background: #d4af37;
        }
        .terms {
            background: rgba(255, 215, 0, 0.1);
            padding: 10px;
            border-radius: 8px;
            font-size: 14px;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Job Application Form</h1>

        <div class="terms">
            <p><strong>Terms:</strong> By submitting this application, you acknowledge that Orb Nexus reserves the right to verify the provided information.</p>
        </div>

        <form action="/submit-job-application" method="POST" enctype="multipart/form-data">
            <label>Party Name (Required)</label>
            <input type="text" name="partyName" required>

            <label>Full Name</label>
            <input type="text" name="fullName">

            <label>Contact Number (Required)</label>
            <input type="tel" name="contactNumber" required>

            <label>Email ID (Required)</label>
            <input type="email" name="emailID" required>

            <label>Upload Resume (Optional)</label>
            <input type="file" name="resume" accept=".pdf">

            <p><strong>Note:</strong> Document submission starts after one month and must be completed within three months.</p>

            <button type="submit">Submit Application</button>
        </form>
    </div>
</body>
</html>
